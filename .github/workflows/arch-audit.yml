name: Architecture Audit

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  arch-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.13.0"

      - name: Install dependencies
        run: npm ci

      - name: Run Architecture Audit (8 Dependency Checks)
        run: node ./.claude/skills/arch-audit/scripts/run_all_checks.mjs
